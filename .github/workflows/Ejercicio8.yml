name: Ejercicio 8

on:
    push:

jobs:
    install-dependecies:
        strategy:
            matrix:
                include:
                    - os: ubuntu-latest
                      node: 20

                    - os: ubuntu-latest
                      node: 22

                    - os: ubuntu-latest
                      node: 24

                    - os: windows-latest
                      node: 22

        runs-on: ${{ matrix.os }}
        steps:
            - name: checkout
              uses: actions/checkout@v4

            - name: Setup node
              uses: actions/setup-node@v4
              with:
                node-version: ${{ matrix.node }}